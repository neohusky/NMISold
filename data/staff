require_once('./data/config.php');
require_once('../codebase/connector/grid_connector.php');


	$grid = new GridConnector($conn);
 	$grid->enable_log("log.txt",true);
 	$grid->render_sql(
        "Select *
         From staff
         Where Username = 'theok'",
        "UserName",
        "Classification,StaffName"
    );